import { createContext } from "react";

// layers, setLayers
// isQueried, setIsQueried
// activeLayers, updateActiveLayers, rearrangeActiveLayers, toggleLayers
 export const LayersContext = createContext(null)
 export const QueriedContext = createContext(null)
 export const ActiveLayersContext = createContext(null)